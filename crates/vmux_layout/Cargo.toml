[package]
name = "vmux_layout"
description = "vmux — hierarchical pane layout tree, solver, and session snapshot types"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
bevy = { workspace = true }
ron = "0.8"
serde = { version = "1.0", features = ["derive"] }
