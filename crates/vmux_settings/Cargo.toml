[package]
name = "vmux_settings"
description = "vmux — shared defaults and app settings (moonshine-persisted with session)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
bevy = { workspace = true }
crossbeam-channel = { workspace = true }
notify = { workspace = true }
ron = "0.10"
serde = { version = "1", features = ["derive"] }
