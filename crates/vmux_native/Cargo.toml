[package]
name = "vmux_native"
description = "Run a vmux page in this process: a Dioxus VirtualDom, painted by a webview of its own"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
base64 = "0.22"
dioxus-core = "=0.7.9"
dioxus-html = { version = "=0.7.9", features = ["serialize"] }
# `binary-protocol` and `sledgehammer` are what gate `MutationState`, the `WriteMutations` sink
# that turns a render into the byte batch `run_from_bytes` applies. Without both, this crate has
# nothing to send.
dioxus-interpreter-js = { version = "=0.7.9", features = [
    "binary-protocol",
    "sledgehammer",
] }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = "0.1"

# The view half, on the two targets that have a window to parent a webview into. Spelled out
# rather than as the `ui` alias because Cargo resolves dependencies before any build script runs;
# `build_platform_cfg.rs` requires the manifest and the alias to say the same thing.
[target.'cfg(any(target_os = "ios", target_os = "macos"))'.dependencies]
objc2-quartz-core = { version = "0.3", default-features = false, features = [
    "std",
    "CALayer",
] }
tokio = { workspace = true, features = ["rt-multi-thread", "time"] }
vmux_clipboard = { path = "../vmux_clipboard" }
vmux_ui = { path = "../vmux_ui", default-features = false }
wry = { version = "0.53", features = ["transparent"] }

[target.'cfg(target_os = "macos")'.dependencies]
objc2-app-kit = { version = "0.3", default-features = false, features = [
    "std",
    "NSAppearance",
    "NSResponder",
    "NSView",
    "NSWindow",
] }

[target.'cfg(target_os = "ios")'.dependencies]
objc2 = "0.6"
objc2-core-foundation = { version = "0.3", default-features = false, features = [
    "std",
    "CFCGTypes",
] }
objc2-ui-kit = { version = "0.3", default-features = false, features = [
    "std",
    "UIInterface",
    "UIResponder",
    "UIView",
    "objc2-core-foundation",
    # `UIView::layer` is gated on this, and `raise_above_layers` is the whole reason we reach for
    # a view at all.
    "objc2-quartz-core",
] }

[dev-dependencies]
dioxus = { workspace = true }

[lints]
workspace = true
