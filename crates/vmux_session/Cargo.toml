[package]
name = "vmux_session"
description = "vmux — session persistence (moonshine-save layout snapshot + URL observer)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
bevy = { workspace = true }
bevy_cef = { workspace = true }
moonshine-save = { workspace = true }
vmux_core = { path = "../vmux_core" }
vmux_input = { path = "../vmux_input" }
vmux_layout = { path = "../vmux_layout" }
vmux_settings = { path = "../vmux_settings" }
vmux_webview = { path = "../vmux_webview" }
