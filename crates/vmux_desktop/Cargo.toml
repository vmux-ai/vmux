[package]
name = "vmux_desktop"
version.workspace = true
edition.workspace = true
description.workspace = true
publish = false

[features]
debug = ["bevy_cef/debug"]

[[bin]]
name = "Vmux"
path = "src/main.rs"

[dependencies]
bevy = { workspace = true, features = [
    "bevy_camera_controller",
    "free_camera",
] }
bevy_cef = { workspace = true }
bevy_cef_core = { workspace = true }

libc = "0.2"
raw-window-handle = "0.6"
bevy_log = "0.18"
directories = "5"
muda = "0.17"
parking_lot = "0.12"
bevy_reflect = { workspace = true }
moonshine-save = { workspace = true }
ron = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
vmux_core = { path = "../vmux_core" }
vmux_history = { path = "../vmux_history" }
vmux_layout = { path = "../vmux_layout" }
vmux_command_bar = { path = "../vmux_command_bar" }
vmux_macro = { path = "../vmux_macro" }
vmux_ui = { path = "../vmux_ui", default-features = false }
tokio = { workspace = true }
uuid = { workspace = true }
vmux_service = { path = "../vmux_service" }
vmux_processes = { path = "../vmux_processes" }
vmux_sessions = { path = "../vmux_sessions" }
vmux_terminal = { path = "../vmux_terminal" }
vmux_webview_app = { path = "../vmux_webview_app" }

rfd = { workspace = true }
notify = { workspace = true }
semver = "1"
cargo-packager-updater = "0.2"

[package.metadata.packager]
product-name = "Vmux"
identifier = "ai.vmux.desktop"
category = "Productivity"
copyright = "Copyright 2024-2025 Junichi Sugiura. MIT License."
homepage = "https://vmux.ai"
formats = ["app", "dmg"]
icons = ["../../packaging/macos/Vmux.icns"]
before-packaging-command = "env -u CEF_PATH cargo build -p vmux_desktop -p bevy_cef_debug_render_process --release"
before-each-package-command = "bash ../../scripts/inject-cef.sh"

[package.metadata.packager.dmg]
background = "../../packaging/macos/dmg-background.png"

window-size = { width = 660, height = 400 }
app-position = { x = 180, y = 180 }
application-folder-position = { x = 480, y = 180 }

[package.metadata.packager.macos]
minimum-system-version = "13.0"
signing-identity = "Developer ID Application: Junichi Sugiura (4BG7FRMF2G)"
entitlements = "../../packaging/macos/Vmux.entitlements"
