# vmux CEF crash reporter config (Crashpad)
# ProductName/ProductVersion left empty: taken from Info.plist
# CFBundleName / CFBundleShortVersionString.
# No ServerURL: crash reports are stored locally only (no upload).
[Config]
ProductName=
ProductVersion=
ServerURL=
RateLimitEnabled=false
MaxUploads=0
BrowserCrashForwardingEnabled=false
[CrashKeys]
